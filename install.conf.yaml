- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- brew:
  - zsh

- link:
    ~/.zshrc: zsh/zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
