- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- brew:
  - zsh

- cask:
  - hyper

- link:
    ~/.zshrc: zsh/zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
